{
  "name": "to-do-webapp",
  "version": "0.1.0",
  "private": true,
  "config": {
    "build_tmp_folder": ".tmp",
    "dist_folder": "dist"
  },
  "main": "$npm_package_config_dist_folder/server.js",
  "scripts": {
    "clean": "shx rm -rf out/ $npm_package_config_dist_folder",
    "dev": "next dev --turbopack",
    "build": "next build && yarn standalone:copy && yarn standalone:clean && yarn standalone:rename",
    "start": "next start",
    "lint": "next lint",
    "standalone:copy": "shx mkdir $npm_package_config_build_tmp_folder && shx cp -r .next/standalone/apps/to-do-webapp/.next/* $npm_package_config_build_tmp_folder && shx mkdir $npm_package_config_dist_folder && shx cp .next/standalone/apps/to-do-webapp/server.js $npm_package_config_dist_folder",
    "standalone:clean": "shx rm -rf .next",
    "standalone:rename": "shx mv $npm_package_config_build_tmp_folder .next"
  },
  "files": [
    ".next"
  ],
  "dependencies": {
    "next": "15.1.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@pagopa/typescript-config-react": "workspace:^",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.1.2",
    "shx": "^0.3.4",
    "typescript": "^5.7.2"
  }
}
