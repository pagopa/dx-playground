name: Legacy Terraform Apply

on:
  push:
    branches:
      - main
    paths:
      - "infra/legacy/**"
  workflow_dispatch:

jobs:
  apply_dev:
    name: Apply Development
    uses: pagopa/dx/.github/workflows/release-terraform-legacy-v1.yaml@infra-legacy-apply-workflow
    secrets: inherit
    with:
      environment: infra-dev
      dir: infra/legacy
      azure_environment: dev
      use_private_agent: true
