name: Release Infra

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      # Trigger the workflow when resources are modified
      - "infra/resources/**"
      # Trigger the workflow when the involved workflows are modified
      - ".github/workflows/code_review_infra.yaml"

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release_dev:
    uses: pagopa/dx/.github/workflows/infra_apply.yaml@1323cb47dc65aadf61a04c4f34bdef49cb8da727 # main
    name: Release Infrastructure
    secrets: inherit
    with:
      environment: dev
      override_github_environment: infra-dev
      base_path: infra/resources
