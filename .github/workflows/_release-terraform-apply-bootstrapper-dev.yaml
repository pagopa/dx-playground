name: Release Bootstrapper Infrastructure - dev
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "infra/bootstrapper/dev/**"

permissions:
  contents: read
  id-token: write

jobs:
  release:
    uses: pagopa/dx/.github/workflows/release-terraform-bootstrapper-v1.yaml@1323cb47dc65aadf61a04c4f34bdef49cb8da727 # main
    name: Release Bootstrapper (dev)
    secrets: inherit
    with:
      environment: 'dev'
