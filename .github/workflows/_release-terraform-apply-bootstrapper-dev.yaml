name: Release Bootstrapper Infrastructure - dev
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "infra/bootstrapper/dev/**"

permissions:
  contents: read
  id-token: write

jobs:
  release:
    uses: pagopa/dx/.github/workflows/release-terraform-bootstrapper-v1.yaml@a18d71062ddaaa09f8e145835a05aefd7701935e # main
    name: Release Bootstrapper (dev)
    secrets: inherit
    with:
      environment: 'dev'
