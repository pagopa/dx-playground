name: Infra Code Review

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - "infra/resources/**"
      - ".github/workflows/code_review_infra.yaml"

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  code_review_dev:
    uses: pagopa/dx/.github/workflows/infra_plan.yaml@ci-ai-phase/06
    name: Code Review Infra Plan
    secrets: inherit
    with:
      environment: dev
      override_github_environment: infra-dev
      base_path: infra/resources
      use_private_agent: true
      terraform_permission_check_enabled: true
      terraform_permission_check_gateway_url: https://dx-d-itn-ai-apim-01.azure-api.net/ai/v1/responses
      terraform_permission_check_model_deployment_name: gpt-5-6-luna
      terraform_permission_check_cd_identity_name: dx-d-itn-playground-infra-github-cd-id-01
      terraform_permission_check_cd_identity_resource_group_name: dx-d-itn-playground-rg-01
      terraform_permission_check_dx_ref: ci-ai-phase/06
