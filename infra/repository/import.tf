# Import blocks for migrating GitHub resources to the new module
# Generated by Azure GitHub Environment Bootstrap migration script
#
# INSTRUCTIONS:
# 1. Run 'terraform plan' to see what will be imported
# 2. Run 'terraform apply' to execute the imports
# 3. After successful import, you can delete this file
#
# NOTE: These import blocks assume the standard PagoPA naming conventions
# If your environments use different names, adjust the import IDs accordingly

# Import the main GitHub repository
import {
  to = module.github_repository.github_repository.this
  id = "dx-playground"
}

# Import additional repository resources
import {
  to = module.github_repository.github_actions_repository_permissions.repo
  id = "dx-playground"
}

import {
  to = module.github_repository.github_branch_default.main
  id = "dx-playground"
}

# Import infra_cd environment for dev
import {
  to = module.github_repository.github_repository_environment.infra_cd["dev"]
  id = "dx-playground:infra-dev-cd"
}

# Import app_cd environment for dev
import {
  to = module.github_repository.github_repository_environment.app_cd["dev"]
  id = "dx-playground:app-dev-cd"
}

# Import opex_cd environment for dev
import {
  to = module.github_repository.github_repository_environment.opex_cd["dev"]
  id = "dx-playground:opex-dev-cd"
}

# Import infra_ci environment for dev
import {
  to = module.github_repository.github_repository_environment.infra_ci["dev"]
  id = "dx-playground:infra-dev-ci"
}

# Import opex_ci environment for dev
import {
  to = module.github_repository.github_repository_environment.opex_ci["dev"]
  id = "dx-playground:opex-dev-ci"
}

